import { SiteResponse } from '../response/SiteResponse';

export type { SiteResponse } from '../response/SiteResponse';

/**
 * Alias de SiteResponse. Se mantiene por compatibilidad.
 * Preferir importar SiteResponse desde 'app/shared/models/response/SiteResponse'.
 */
export type Site = SiteResponse;
